# Gate 2 Outlier Analysis

**Status:** TEMPLATE / NO RESULTS

This file defines the reporting structure only. It contains no observed Gate-2 data.

## Predeclared method

Primary flagging method: IQR-based rule, applied according to the frozen statistical specification. The exact grouping and threshold must be frozen before execution.

## Results

| Field | Value |
|---|---|
| Planned executions | 33 |
| Verified executions | NOT VERIFIED |
| Outliers identified | NOT AVAILABLE |
| Outlier rate | NOT AVAILABLE |

## Per-outlier record

For every flagged observation, report:

- seed;
- repeat/execution identifier;
- metric value;
- threshold;
- provenance link;
- reason for flag;
- retain/exclude decision;
- effect on sensitivity analysis.

## Exclusion rule

The primary analysis must retain flagged observations unless a different rule is frozen before execution. Any exclusion requires explicit justification and a sensitivity analysis.

## Decision

`NOT EVALUATED — NO GATE-2 RESULTS VERIFIED`
